apiVersion: v1
kind: Service
metadata:
  name: grafana-service
  namespace: docker-influxdb-grafana
spec:
  type: ClusterIP
  ports:
    - name: "grafana-port"
      targetPort: 3000
      port: 3000
  selector:
    app: grafana
